rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all read/write access (read includes both get and list/query)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}